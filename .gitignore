# ── Rust / Cargo ──────────────────────────────────────────────────────────────
/target/

# Incremental compilation artifacts
**/*.rs.bk

# Profiling / coverage
*.profraw
*.profdata
lcov.info
tarpaulin-report.html
tarpaulin-report.json

# ── IDE / Editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.iml
*.swp
*.swo
*~
.DS_Store
tmp
# ── System ────────────────────────────────────────────────────────────────────
Thumbs.db
desktop.ini

# ── Old C++ build artifacts (now removed, but guard against accidental regen) ──
build/
*.o
*.obj
# Compiled binary artifacts — root-only (prevents matching crates/browser/)
/browser
/browser.exe

# ── Ferrum-specific generated files ───────────────────────────────────────────
# Browser profile data — always in-memory by default, never committed
profile/
*.ferrum-cache
*.ferrum-cookies

# Fuzz corpus and artifacts
fuzz/target/
fuzz/corpus/
fuzz/artifacts/

# Benchmark output
criterion/

# ── AI agent working directory ─────────────────────────────────────────────────
.agents/
.agent/
Rules